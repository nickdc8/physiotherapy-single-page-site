import styled from "@emotion/styled"
import { color, textStyle, space, typography } from "styled-system"

export const Canon = styled.h1(textStyle, color, space, typography)
export const Trafalger = styled.h1(textStyle, color, space, typography)
export const BodyCopy = styled.p(textStyle, color, space, typography)
